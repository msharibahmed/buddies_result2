import 'package:flutter/material.dart';

class ResultNames extends StatelessWidget {
  static const routeName = 'result-names';
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
